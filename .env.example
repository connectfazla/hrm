# Web
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000/api/v1

# API
DATABASE_URL=postgresql://uppearance:uppearance@localhost:5432/uppearance_hrms?schema=public
APP_BASE_URL=http://localhost:4000
WEB_BASE_URL=http://localhost:3000

# Auth / security
JWT_ACCESS_SECRET=change-me
JWT_REFRESH_SECRET=change-me
JWT_ACCESS_TTL_SECONDS=900
JWT_REFRESH_TTL_SECONDS=1209600
SESSION_IDLE_TTL_SECONDS=1800

# Files (local encrypted vault)
FILES_STORAGE_ROOT=storage
FILES_MASTER_KEY=change-me-32bytes-min

# Email (local Mailhog defaults)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=
SMTP_PASS=
SMTP_FROM="Uppearance HRMS <hrms@uppearance.local>"
